 let a=[ 'Abi', 23, 'ooty'];
 let b = new Array('Siva', 24, 'chennai');
//console.log(b[1]);
//console.log(a.length);
a.age = 26;
a.push('India');
/* a.pop(); a.shift(); a.unshift('hello');
console.log(a.shift();
for(let i=0; i<a.length; i++){
   console.log (a[i]);
    }
 for(let x of a){
        console.log(x);
    }
let mat=[
    [1,2,3]
    ,[4,5,6]
   ,[7,8,9]]
   console.log(mat[1][1]);
    for(let i=0; i<mat.length; i++){
     for(let j=0; j<mat[i].length; j++){
          console.log(mat[i][j]);
     }
    } */
   // Map data structure; exactly like objects;
  
  /* let map = new Map();

   map.set(1, 'name',)
      .set(2, 'age')
      .set('logic', 'place');
    console.log(map.get(1));
    console.log(map.has(2));

map.delete(2); for delete one key value pair;
map.clear(); for clear all key value pair;
console.log(map.size); // for size of map;
for(key of map.keys()){
    console.log(key);
}
*/

/*let set = new Set();
set.add('code io')
  .add('logic io')
  .add('code io');// duplicate values are not allowed;
console.log(set.has('code io'));// true or false;
console.log(set.size);
set.delete('code io');
set.clear();
*/
